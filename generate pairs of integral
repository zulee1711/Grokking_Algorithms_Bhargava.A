# With a given integral number n, write a program to generate a dictionary that contains (i, i*i) such that is an integral number between 1 and n (both included)
n = int(input("Value input: "))
temp = dict()
for i in range (1, n+1):
    temp[i] = i*i
print(temp)
